# Supabase credentials only (optional - you can use a single .env with all vars)
# Copy the values below into your main .env file.
# Source: Supabase Dashboard → Project Settings → API

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.YOUR_ANON_KEY

# Where to find:
# - Project URL     → Supabase Dashboard → Project Settings → API → Project URL
# - anon public key   → Project Settings → API → Project API keys → anon public
